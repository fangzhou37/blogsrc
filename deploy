hexo clean; 

hexo g; 

cd ../gitIO;

./cleanup;

cd -;

cp -a public/. ../gitIO/ ;

cd -;

git add .;

time_stamp=$(date +%Y-%m-%d-%H-%M-%S)

git commit -m "deploy: $time_stamp"

git push -u origin master

cd -
